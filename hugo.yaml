# ─────────────────────────────────────────────────────────────
# xiaowen 主题默认配置
# 这里只放「通用默认值」；站点（或 exampleSite）的 params 会覆盖同名项。
# ─────────────────────────────────────────────────────────────

params:
  toc: true
  homePosts: 6

  # 顶部导航：某项注释掉即隐藏；icon 可选 home / posts / tags / projects / links / about
  # （默认只放一定存在的页面；项目/友链/关于需要你自己有对应内容页）
  nav:
    - name: 首页
      url: /
      icon: home
    - name: 博客
      url: /posts/
      icon: posts
    - name: 标签
      url: /tags/
      icon: tags

  # 首页「个人作品」入口的标题（项目内容在 content/projects/ 下）
  works:
    eyebrow: FEATURED WORK
    title: 个人作品
    count: 3

  # 首页「技术栈」：enable: true 时按 groups 结构填充
  techstack:
    enable: false

  # 资源
  assets:
    pixelFont: "https://cdn.jsdelivr.net/npm/@fontsource/fusion-pixel-12px-proportional-sc@5.3.0/index.css"

# ── 以下为可覆盖项示例（在站点/exampleSite 里写同名 params 即可）──
# params:
#   identity:
#     name: 你的名字
#     logo: images/logo.png
#     favicon: images/logo.png
#   hero:
#     eyebrow: 一句话标签
#     title: 你的名字
#     tagline: 一句介绍
#     intro: |
#       支持 Markdown 的自我介绍
#   social:
#     github: https://github.com/you
#   links:
#     title: 友情链接
#     items:
#       - name: 朋友
#         url: https://example.com
#         description: 朋友的站点
